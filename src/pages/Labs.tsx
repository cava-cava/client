import React from 'react';
import {Link} from "react-router-dom";
import Timer from '../components/Timer/Timer'

const Labs = () => {
    return (
        <div className="Labs">
            <h1>LABS</h1>
            <Timer />
        </div>
    );
}

export default Labs;
