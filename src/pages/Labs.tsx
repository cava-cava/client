import React from 'react';
import Cards from '../components/Cards/Cards'

const Labs = () => {
    return (
        <div className="Labs">
            <h1>LABS</h1>
            <Cards />
        </div>
    );
}

export default Labs;
